<%= form_for([:admin, @profile_question]) do |f| %>
  <% if @profile_question.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@profile_question.errors.count, "error") %> prohibited this admin_profile_question from being saved:</h2>

      <ul>
      <% @profile_question.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :short_display %><br />
    <%= f.text_field :short_display %>
  </div>
  <div class="field">
    <%= f.label :long_display %><br />
    <%= f.text_field :long_display %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
